# This directory holds static assets Remotion can resolve via staticFile().
#
# - {phase}-narration.wav    — generated by `pnpm narrate:{phase}`
# - captures/                — generated by `pnpm record:browser` and `pnpm record:terminal`
#
# Keep this directory committed (via .gitkeep), but its contents are gitignored.
